import styled from "styled-components"

export const SectionStyled = styled.section(() => {
    return {
        padding: '10px 0',
        h2: {
            margin: '10px 0',
            fontWeight: '500'
        }
    }
})
